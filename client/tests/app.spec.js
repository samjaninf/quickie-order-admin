describe('Unit: App', function () {
    it('tests if Karma is wired up', function() {
        expect(1).toEqual(1);
    });

    it('should watch this file for changes', function() {
        expect(2).toEqual(2);
    });
});